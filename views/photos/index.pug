extends ../layout.pug
include ./mixins.pug

block title
  title Homepage

block body
  +sideNav()
  div#featuredPicture
    h2 Latest Post
    -const route = '/gallery/' + one.id
    a(href=route)
      img(src=one.link)
  div#picturesContainer
    h2 Photo Gallery
    +photoList(data,one)